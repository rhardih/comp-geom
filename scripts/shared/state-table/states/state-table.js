var State = require('ampersand-state');

//------------------------------------------------------------------------------

var StateTable = State.extend({
  props: {
    rows: 'array'
  }
});

module.exports = StateTable;
